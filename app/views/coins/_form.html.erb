<%= form_for [chest, coin] do |f| %>
  <%= f.number_field :value %> <br>
  <%= f.select :category_id, transform_for_select(categories, :id, :name), { include_blank: true } %> <br>
  <%= f.date_field :created_at %>


  <%= f.submit 'Create' %>
<% end %>
