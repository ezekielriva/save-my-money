<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "#{current_user.email}'s Wallet", root_path %></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon">
      <a href="#"><span>Menu</span></a>
    </li>
  </ul>
  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">
      <li class="<%= active_menu(controller_name, 'chests') %>">
        <%= link_to 'My Chests', chests_path %>
      </li>
      <li class="<%= active_menu(controller_name, 'coins') %>">
        <%= link_to 'Coin flow', coins_path %>
      </li>
      <li class="<%= active_menu(controller_name, 'categories') %>">
        <%= link_to 'Categories', categories_path %>
      </li>
    </ul>

    <!-- Right Nav Section -->
    <ul class="right">
      <li class="has-dropdown">
        <a href="#">Right Button Dropdown</a>
        <ul class="dropdown">
          <li>
            <a href="#">First link in dropdown</a>
          </li>
        </ul>
      </li>
      <li>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
      </li>
    </ul>
  </section>
</nav>
