<%= form_for [chest, coin], html: { class: 'small-12 column'} do |f| %>
  <%= render "shared/error_messages", m: coin %>
  <%= render "default_form_fields", coin: coin, f: f %>

  <%= f.submit (is_new)? 'Create': 'Edit', class: 'button success text-center small-12' %>
<% end %>
